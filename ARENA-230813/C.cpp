#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n;
    long long l;
    vector<long long> vl;
    for (int i = 0; i < n; i++)
    {
        cin >> l;
        vl.push_back(l);
    }
}
