#include <iostream>
#include <vector>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n, q, d, s, e, k;
    vector<int> v;
    cin >> n >> q;
    for (int i = 0; i < q, i++)
    {
        cin >> k;
        v.push_back(k);
    }
}
/////