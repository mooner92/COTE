#include <iostream>
#include <string>
using namespace std;
int main(){
    int tc;
    cin>>tc;
    for(int i=0;i<tc;i++){
        int n;
        string s;
        cin>>n>>s;
        char k = s[0];  //string 의 첫번째 문자를 k에 저장
        if(k==')'){ //닫침괄호로 시작하면 완전조합은 만들 수 없음 펠린드롬만 가능
            
        }

        

    }
}